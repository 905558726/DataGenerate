# ============================================================
# DataGenerate - 中文电商模拟数据生成工具 依赖文件
# ============================================================
# Python 版本要求: >= 3.8
# 安装方式: pip install -r requirements.txt
# ============================================================

# 核心依赖（标准库，无需额外安装）
# - random, json, uuid, argparse, os, sys, time
# - datetime, pathlib, collections, re

# --- 可选依赖 ---

# YAML 配置文件支持（无安装时自动回退到 JSON 配置）
pyyaml>=6.0

# Kafka 生产者支持（仅在 --output kafka://host:port/topic 模式时需要）
# 推荐 confluent-kafka（基于 C 库 librdkafka，与企业 Kafka 完全兼容）
# 备选 kafka-python（纯 Python 实现，协议层面兼容 Kafka 3.8.0）
confluent-kafka>=2.0
# kafka-python>=2.0    # 与 confluent-kafka 二选一，优先前者
