# ============================================================
# DataGenerate - 中文电商模拟数据生成工具 依赖文件
# ============================================================
# Python 版本要求: >= 3.8
# 安装方式: pip install -r requirements.txt
# ============================================================

# 核心依赖（标准库，无需额外安装）
# - random, json, uuid, argparse, os, sys, time
# - datetime, pathlib, collections

# 可选依赖（增强 YAML 配置文件支持）
# 无 pyyaml 时自动回退到 JSON 配置，不影响核心功能
pyyaml>=6.0
