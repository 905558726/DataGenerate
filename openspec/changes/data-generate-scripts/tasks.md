# Tasks: Data Generate Scripts

## 1. 项目基础搭建

- [ ] 1.1 创建项目目录结构（`scripts/`、`data/`、`output/`）和空白 `__init__.py` 文件
- [ ] 1.2 安装依赖检查：无强制第三方依赖（纯标准库），可选 `pyyaml`（无安装时自动回退 JSON 配置）
- [ ] 1.3 创建 `config.yaml` 配置文件，包含：商品/订单生成数量、输出路径、随机种子、商品库路径等默认参数
- [ ] 1.4 创建 `output/.gitkeep` 确保输出目录被 git 跟踪

## 2. 预置商品库 (data/product_library.json)

- [ ] 2.1 构建商品库框架：确定 JSON Schema，每个条目包含 name, brand, category, sub_category, price_min, price_max, image_url, keywords, variant_suffixes
- [ ] 2.2 填充"电子产品"分类商品（手机、笔记本电脑、平板、耳机、智能手表等，40-50 条，价格参考真实行情）
- [ ] 2.3 填充"服装"分类商品（男装、女装、童装、内衣、运动服饰等，40-50 条）
- [ ] 2.4 填充"食品饮料"分类商品（休闲零食、饮料冲调、生鲜水果、粮油调味等，30-40 条）
- [ ] 2.5 填充"家居生活"分类商品（家纺、家具、厨具、收纳用品、灯具等，30-40 条）
- [ ] 2.6 填充"美妆个护"分类商品（护肤、彩妆、洗护、口腔护理等，30-40 条）
- [ ] 2.7 填充"母婴"分类商品（奶粉辅食、纸尿裤、童车童床等，20-30 条）
- [ ] 2.8 填充"运动户外"分类商品（运动鞋、健身器材、户外装备、骑行等，20-30 条）
- [ ] 2.9 填充"图书文娱"分类商品（小说、教辅、少儿图书、文具等，20-30 条）
- [ ] 2.10 验证商品库：确认 JSON 格式正确、无重复名称、category 一致性、条目总数≥200

## 3. 公共工具模块 (data_utils.py)

- [ ] 3.1 实现中文姓名生成器（姓氏列表 + 名字列表，支持单姓/复姓，性别区分）
- [ ] 3.2 实现手机号生成器（符合中国手机号格式：1[3-9]xxxxxxxxx）
- [ ] 3.3 实现邮箱生成器（基于姓名拼音首字母 + 常见邮箱域名 @qq.com/@163.com/@gmail.com）
- [ ] 3.4 实现收货地址生成器（省市区三级联动真实数据 + 详细地址随机模板 + 6 位邮编匹配城市）
- [ ] 3.5 实现支付方式列表和物流公司列表的数据字典（含运单号前缀规则）
- [ ] 3.6 实现通用随机工具函数：加权随机选择、范围内的随机浮点/整数、随机日期时间（指定天数范围内）、UUID 短ID 生成
- [ ] 3.7 实现配置加载 + 命令行解析函数：优先 yaml，回退 json；支持 --count, --seed, --output, --library 等公共参数

## 4. 商品生成器 (product_generator.py)

- [ ] 4.1 实现商品库加载与验证函数（检查必填字段、分类统计、数量下限检查）
- [ ] 4.2 实现单条商品生成函数：从库条目采样 → 决定是否生成变体 → 生成 price/stock/sales_volume/description/product_id/created_at
- [ ] 4.3 实现变体生成逻辑：variant_suffixes 随机选取后缀拼接 name → 价格在 price_min~price_max 内微调 → 标记 is_variant=true
- [ ] 4.4 实现模拟字段生成：stock（80%:100-9999, 15%:1-99, 5%:0），sales_volume（与 stock 正相关），description（品牌+名称+关键词拼接成句）
- [ ] 4.5 实现采样策略：count ≤ 库大小时无放回随机采样；count > 库大小则全量采样 + 变体扩展填充差额
- [ ] 4.6 实现 product_id 唯一性校验（生成后全量查重）
- [ ] 4.7 实现 `main()` 入口：解析参数 → 加载配置 → 加载商品库 → 生成 → 输出 JSON → 打印摘要（总量、各分类数量、直采数/变体数、耗时）

## 5. 订单生成器 (order_generator.py)

- [ ] 5.1 实现商品 JSON 文件加载函数（支持 `--products` 指定路径，默认 `output/products.json`）
- [ ] 5.2 实现 `select_order_items(products)` 函数：随机选取 1-5 个商品，quantity 加权分布（1~3 占 70%，4~6 占 20%，7~10 占 10%）
- [ ] 5.3 实现 `generate_buyer()` 买家信息生成（复用 data_utils 姓名/手机/邮箱）
- [ ] 5.4 实现 `generate_shipping_address()` 收货地址生成（复用 data_utils 地址生成器 + 收件人姓名/电话）
- [ ] 5.5 实现订单状态按权重分配（已完成 50%、已发货 15%、已付款 15%、待付款 10%、已取消 10%）
- [ ] 5.6 实现支付信息生成：支付方式（微信支付/支付宝/银行卡/信用卡 按权重随机）+ paid_at 与订单状态一致性校验
- [ ] 5.7 实现物流信息生成：物流公司随机 + 运单号生成（前缀+时间戳+随机数字）+ shipped_at 仅已发货/已完成时有值
- [ ] 5.8 实现订单金额计算（items subtotal 求和 → order_amount → 随机折扣 0~15% → actual_amount）
- [ ] 5.9 实现订单备注随机生成（85% null，15% 随机备注如"请尽快发货""易碎品""工作日收货"等）
- [ ] 5.10 实现单条订单 `generate_order(order_id, products)` 完整生成 + order_id 唯一性校验
- [ ] 5.11 实现批量生成 + 时间分布（默认近 90 天内随机分布）
- [ ] 5.12 实现 `main()` 入口：解析参数 → 加载配置 → 加载商品数据 → 批量生成 → 输出 JSON → 打印摘要

## 6. 集成和验证

- [ ] 6.1 端到端流程：product_generator.py --count 100 → order_generator.py --count 50 → 检查输出文件完整性
- [ ] 6.2 验证商品 JSON：12 个字段完整、product_id 无重复、is_variant 标记正确、price 在合理范围、description 长度≥10
- [ ] 6.3 验证订单 JSON：12 个字段完整、order_id 无重复、items 中 product_id 均存在于商品文件、状态-支付-物流一致性
- [ ] 6.4 测试 `--seed` 可复现性：同种子两次运行输出完全一致
- [ ] 6.5 测试变体生成：count=1000（大于库条目数）时确认所有 ID 唯一、变体数量正确
- [ ] 6.6 测试 `--count`、`--output`、`--library` 命令行参数以及 config.yaml 配置回退逻辑
